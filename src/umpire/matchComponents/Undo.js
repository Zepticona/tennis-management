import React from 'react';

const Undo = (props) => {
    
    return (
        <section className="undo">
            <button className="match-btn--large undo-btn undo-btn--active">undo</button>
        </section>
    )
};

export default Undo;