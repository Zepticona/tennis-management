import React from 'react';

const Correction = (props) => {
    return (
        <section className="correction">            
            <button className="match-btn--large correction-btn grey-btn">Correction</button>
        </section>
    )
};

export default Correction;