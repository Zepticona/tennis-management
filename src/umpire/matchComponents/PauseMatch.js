import React from 'react';

const PauseMatch = (props) => {
    return (
        <section className="pause-match">            
            <button className="match-btn--large pause-btn grey-btn">Pause Match</button>
        </section>
    )
};

export default PauseMatch;