import { useState } from "react";

const useIntervals = () => {
    const [isChangingOver, setIsChangingOver] = useState(55);
    const [pauseSet, setPauseSet] = useState(120);
    const [medicalTimeout, setMedicalTimeout] = useState(false);
    const [tenMinPause, setTenMinPause] = useState(false);
    const [shotClock, setShotClock] = useState(false);

    const handleChaningOver = () => {
        
    }



}

export default useIntervals;